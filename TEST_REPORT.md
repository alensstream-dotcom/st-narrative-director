# 验收记录（2026-09-23）

## 结论

这是一份经过真实 API 与 ComfyUI 联调的候选发布版，不是“所有画面都能完美服从剧情”的承诺。主链路、衣服关联、流式调度和移动布局已多轮验证；Anima 的画面空间关系、Android 真机手势、正式安装持久化仍有限制。

## 测试环境

- SillyTavern `http://localhost:11451/`；测试通过独立 Playwright Edge 上下文把当前工作区源码路由到页面，不修改聊天正文，也不保存测试设置。
- 原版智绘姬 3.1.2 可读取；没有修改智绘姬文件。
- 当前 ComfyUI `http://127.0.0.1:8188`；`miaomiaoHarem_29BBETA10.safetensors`、`Miaomiao Harem · 配套加速工作流`、12 步、CFG 2.5、Euler/simple、896×1152、LoRA 0.8、shift 3。
- 多轮端到端使用用户指定的 `gpt-6-sol` 兼容 API。密钥只注入隔离浏览器会话，不写入仓库、聊天或设置，也不在记录中输出。

## 已验证

- `npm test`：47 项通过。覆盖英文提示词、原文锚点、编辑/Swipe/聊天隔离、无原文写入、长程人物状态、服装证据与去重、智绘姬档案修改保护、12 步工作流、重试与错误分类、Anima 正脸策略和 ANIMADEX 筛选链接。
- 多轮真实手动流程：选择跨段剧情后返回两个不同事件；用户选择雨伞事件后，确认前图片请求为 0，确认后仅发 1 次。图片绑定到所选文字下方，原有四个剧情选项仍在；同人物的有证据黑外套在确认后关联到智绘姬对应人物预设。预览/取消阶段不创建服装。
- 多轮真实自动流程：相机事件在 `round.final === false` 时已提交；回复结束后的相邻后续动作没有凑出重复图，自动槽位最多 1 张。确认的人物发色、发型、眼睛与特征被下一镜复用；Swipe 后旧图隐藏。
- 正文未被写入 prompt、按钮或记忆标签；测试检查 `mes` 保持原样。导演提示词全英文并明确禁止翻译/虚构对白、文字气泡。用户正式聊天未被测试消息污染。
- 针对真实失败样本加入 Anima 专用人脸/道具构图约束。最新真实自动成图可见正脸、双眼、黑衣、相机；手动独立复测也出现完整面部。曾出现相机遮眼和画中画，已通过消除“raised camera”冲突、把相机放在下巴以下和负向约束改善，但模型仍可能随机违背。
- ANIMADEX 实测支持 36,488 个角色数据及发色/眼睛/发长/性别筛选；`silver hair + green eyes + short hair + 1girl` 返回 5 个候选。酒馆页面跨域 fetch 被浏览器拦截，所以人物页改为生成筛选链接，不在每张图片生成时请求外站。
- 360、430、1280px 设置面板及 API/生图/人物/任务页均无横向溢出，未捕获插件 JavaScript 异常。跨段触屏选择模拟能打开 prompt 确认框，取消时无图片请求。
- 新安装默认预填用户指定 API 地址和 `gpt-6-sol`，自动导演仍默认关闭；公开仓库不含 API 密钥。

## 已知限制

- 当前桌面测试是路由注入工作区源码，不等于已从 GitHub 安装并持久化到用户正式酒馆；原环境曾因 `Program Files` 权限导致 ST 设置写入失败。发布后仍需确认正式安装/设置保存。
- 触屏选区是浏览器自动化模拟，不是 Android 真机长按、拖选或手机局域网验收。
- 智绘姬的私有队列、缓存和查看器没有公开的逐任务配置接口。插件读取/写入其人物与服装档案格式，但生图走酒馆/ComfyUI 独立任务，不会修改原版智绘姬，也不共享其私有查看器。
- ANIMADEX 筛选与 Tag 确认是一次性的可选人物设置，不是自动下载其完整数据库或 LoRA。离线原型库只有 10 项；没有适合的候选时以外貌描述为主。
- 人物锁是固定外貌文字和已确认 Tag，不是人脸参考或 LoRA。当前工作流没有已验证的参考图分支；跨图脸型、微小特征、相机/伞的精确朝向与平台空间关系无法保证。Anima 官方也说明当前基础模型没有审美微调，且不以一致性为优化目标。
- 真实 API 分析本轮观察到约 19–34 秒，ComfyUI 约 28–33 秒；调度在正文生成结束前开始，不会阻塞正文，但短回复下图片不一定能在文字结束前完成。
- 关闭自动导演可避免自动上传剧情片段。启用后会把新生成片段发送到辅助 API，并消耗 API/GPU 资源。

## 不代表

上述结果不等于“完美还原率”“Android 真机通过”“原版智绘姬内部任务队列集成”或“用户正式安装已完成”。发布前仍须核对 GitHub 推送、仓库内容无密钥，并尝试通过酒馆扩展管理进行正式安装。

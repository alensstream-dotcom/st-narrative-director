# 验收记录

## 0.4.1 候选版续接验收（2026-09-25，未完成两轮验收）

- 70 项本地回归通过。新增覆盖已知女性首图识别、首图后人物/服装元数据合并、结束时禁止追加尾图，以及正文耗尽思考额度但没有可见剧情的情况。
- 查明酒馆 1.18.0 会按内置模型白名单过滤顶层 `reasoning_effort`。兼容接口现通过 `custom_include_body` 转发该字段。中转站实测接受 `none`、`low`，拒绝 `minimal`；自动及兜底用 `none`，手动用 `low`。
- 修复测试脚本中文输入被字面 Unicode 转义串覆盖的问题；模型设置必须等 settingsReady、保存请求成功并刷新复核。实际自动模型已确认持久保存为 `gpt-6-sol`，正文仍为用户原有 `deepseek-flash`。
- 最后一组第一轮通过：提示词在正文结束前 4.49 秒交付；智绘姬回调完成，图片实际加载为 896×1152。回调在正文结束后约 28.38 秒发生，不能称为图片在正文结束前完成。ComfyUI 原图 `NarrativeDirector_00083_.png` 已人工查看；聊天容器截图出现空白，需要继续核对显示/截图范围。
- 同组第二轮未通过：正文 API 的 29,918 个思考帧合计 84,328 字符，正文内容帧为 0，结束原因为 `length`。没有正常流式正文可供导演选图。不能将该轮记成插件端到端成功，也不能通过绘制思考草稿来凑图。
- 该组测试新增的四条聊天消息已清理，原有五条保留；测试人物/服装资料恢复。脱敏时序证据保存在本地忽略文件 `artifacts/acceptance-main-reasoning-exhausted.json`。
- 当前仍待用户选择是否调低正文模型推理强度，然后重新进行连续两轮实际对话验收、真实安装验证与发布。

## 0.4.0 历史验收（2026-09-24）

### 已通过

- `npm test`：51 项通过。覆盖原文锚点、英语提示词、人物/服装证据、100 层后状态、正脸、自动第二张保留、最终兜底、智绘姬预设保护与回退、消息正文不变和完成回调。
- Edge 430px 移动视口的酒馆页面加载无导演脚本错误；API/生图/人物/任务面板无横向溢出。
- `tools/chatu-dom.cjs`：同一回复两段各出现一个智绘姬生成按钮；模拟酒馆重画后仍各一个，原有两段正文未被修改。
- `tools/chatu-manual.cjs`：430px 触屏视口模拟选中第二段，预览显示准确原文和英文 prompt；确认后只在第二段下出现一个智绘姬按钮，原文不变。页面级禁用自动点击，故此项没有再次消耗 GPU。
- `tools/chatu-integration.cjs --wait-image`：智绘姬识别 `image###...###` 并自动点击；其专用 Miaomiao Harem 工作流经酒馆代理到 ComfyUI，任务历史显示成功；智绘姬回调成功，原文下方图片容器得到 896×1152 图片。没有改动智绘姬源文件。
- 在真实 `gpt-6-sol` 导演 API 上，剧情对白只转成表情/动作，不生成虚构气泡文本；面向镜头与双眼约束正确进入提示词；通用校服和有颜色/徽章的具体款式被正确区分。分析耗时样本约 15–32 秒，正文生成不等待它。
- 当前酒馆辅助 API 已保存为兼容接口 `https://api-slb.krill-code.net/v1`、`gpt-6-sol` 和酒馆密钥引用；连接测试真实成功，密钥内容不在仓库。智绘姬已选中 `叙景 Miaomiao Harem`，自动点击开启，永久自动点击关闭。原有工作流与人物/服装预设未删除。
- 曾由测试回调保存到用户现有聊天的一条专用测试消息已精确删除并重新保存；刷新后的聊天保留原有三条消息，无测试标记。

### 仍需实测

- 未在安卓真机验证长按跨段选区、手机局域网访问和智绘姬设置导出/导入。桌面移动视口不等于真机通过。
- 本轮验证了合成消息与真实导演 API/ComfyUI，不曾在用户的真实角色对话中让正文模型完整输出一轮，因此“每轮至少一图”的真实回复成功率尚不能宣称 100%。导演只在有可靠原文画面且服务可用时兜底出图，不会为保数量捏造画面。
- Miaomiao/Anima 对空间关系、服装开合与跨图人脸一致性仍有模型随机性。当前固定外貌与 Tag 锁定不是参考图像素级锁脸。
- 智绘姬模型下拉框仅提供 checkpoint，可能仍显示旧值；本次 ComfyUI 任务历史已核实真正加载的是 `miaomiaoHarem_29BBETA10.safetensors` UNet。

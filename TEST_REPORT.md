# 验收记录（2026-09-25）

`npm test`：82 项通过。`git diff --check` 通过。下面的真实联调使用本机 SillyTavern 1.18.0、智绘姬 3.1.2、已保存的辅助导演模型 `gpt-6-sol`，以及 ComfyUI 工作流中的 `miaomiaoHarem_29BBETA10.safetensors`。正文角色扮演模型的推理强度保持 `max`，本轮没有下调。

## 连续人物与换装

使用同一人物“青璃”跑完整的三段剧情，均经过真实辅助导演分析和 ComfyUI 生图。三张图使用同一个 ANIMADEX 触发串 `the shorekeeper (wuthering waves), wuthering waves`，辅助导演抽取的固定外貌均为蓝色长发、紫色眼睛。

| 剧情 | 导演传给生图模型的服装 | 实图检查 |
| --- | --- | --- |
| 海边石桥，明确穿深蓝连帽外套和黑裤 | 深蓝连帽外套、黑裤 | 蓝发紫眼、深蓝连帽外套，黑裤仅局部可见 |
| 后文走到花园，未提换装 | 沿用深蓝连帽外套、黑裤 | 蓝发紫眼，深蓝连帽外套和黑裤仍在 |
| 回屋明确换上白色长裙与浅蓝披肩 | 白色长裙、浅蓝披肩 | 蓝发紫眼，白裙与浅蓝披肩可见，旧外套未出现 |

本地预览：[第一张](artifacts/qa-continuity-1.png)、[第二张](artifacts/qa-continuity-2.png)、[第三张](artifacts/qa-continuity-3.png)。这些测试图片留在被 Git 忽略的本地目录，未上传公开仓库。文字 Tag 和提示词保证所选外貌与衣服被提交，不保证每次采样产生像素完全一致的脸。

历史服装现从有原文锚点的已交付镜头和状态更新重建，不把仅存在于智绘姬衣柜里的预设当作当前穿着。自动流式交付时，未换装镜头会带上已证实服装；本段提到衣服时等待完整分析，以免旧衣服抢先提交。对应测试覆盖长距离重现、明确换装、无依据的模型猜测、滑动替换旧镜头和自动提前交付。

## 人物目录、智绘姬保存与手机界面

本地 ANIMADEX 目录包含 36,492 条角色元数据，可搜索并手选触发串；“守岸人”等不在旧示例名单中的角色也可选。360px 触屏视口检查了目录搜索、选择和横向布局。真实生成的三名不同角色图已分别经“保存形象”写入智绘姬对应人物照片、服装照片与服装文本；改用酒馆服务器配置图片存储后刷新页面，三组关联仍可读取。验收产生的测试人物、服装、媒体关联和测试聊天消息已清理；预览图保留在本地忽略目录。

手机设置页能看到 API 地址、Key、模型下拉与手填 ID。已保存辅助密钥在安装状态真实读取到 6 个模型。对兼容接口代理返回 HTTP 200 错误包的情况，插件识别错误、短暂重试，并在当前页面输入的临时密钥可用时尝试直连 `/models`；读取目录不会主动保存全局设置。360px 模拟检查不等于 Android 真机验收。

## 已知边界

先前的真实回复中，导演曾在正文结束前交付提示词，但另一次短回复是在结束后约 1 秒交付；因此不能承诺每轮都早于正文结束。正文若只产生隐藏思考、没有可见剧情，导演不会编造画面。远端模型和 ComfyUI 的可用性、采样时人物脸型或道具细节仍会波动。

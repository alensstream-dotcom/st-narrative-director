# 测试记录（开发验证版）

日期：2026-09-23。不是任务书完整验收通过声明。

## 环境与边界

- SillyTavern 1.18.0、原版智绘姬 3.1.0；没有修改原版文件。
- 独立 Playwright Edge、430×900 移动视口；不是用户当前 Edge 标签页，也不是 Android 真机。
- 真实导演服务：DeepSeek flash，使用酒馆已保存密钥的 ID。未把密钥写入仓库。
- 真实 ComfyUI：Miaomiao Harem 29BBETA10；配套 `anima加速.json` 的节点与参数。12 步、CFG 2.5、Euler/simple、896×1152、Turbo LoRA 0.8、shift 3 / flow。
- 测试使用虚构的成年女性车站剧情，不保存用户的聊天/设置；测试生成的图片会保存在酒馆图片目录和 ComfyUI 输出目录。

## 已运行

`npm test`：35 项单元测试。覆盖原文/思考分离、选段定位、英文词长、动作状态、自动性别规则与名额、第二候选留至回复结束且不倒序补画、取消晚到响应、换聊天/Swipe/编辑隔离、超过 100 条消息的人物状态、原档案修改保护、人物与服装关联、证据门槛、跨人物服装隔离、服装去重、描述锁定、工作流参数、缺失模型拒绝替换、重绘不重复叠加画风。

`node tools/browser-test.cjs --real`：真实直连链路，手动一图、锁定描述后下一轮自动一图，共两张。手动确认前图片请求为零；自动任务在模拟流式回复结束事件之前提交；两图均保存；原有四个剧情按钮保留；切换 Swipe 后旧图不再显示；没有页面 JavaScript 异常。

`node tools/browser-test.cjs --real --proxy`：浏览器回环直连权限受限时，真实酒馆代理回退。同样完成手动一图、自动一图和 Swipe 隔离，共两张。预期出现直连权限失败控制台信息，但代理任务成功，页面无 JavaScript 异常。导演分析约 3 秒，生图约 30 秒，非速度保证。

`node tools/browser-test.cjs --settings`：实际导演 API 测试。输入错误密钥、执行失败的连接测试、继续修改密钥、清除、切回酒馆已保存密钥、成功连接、刷新到 3 个可选模型；人物标签页显示智绘姬档案。测试输入只在本页内存中。

`node tools/browser-test.cjs --real --multi`：跨两段选择，导演返回两个不同剧情镜头；手动选择黑衣红伞场景，确认前无生图请求，确认后出图在选段下方。随后锁定外貌，下一轮流式模拟中自动提交窗边相机图，图片在对应段落下；四个原选项仍可点击，切换 Swipe 后图片不串楼。最新人工看图发现自动图漏画了相机，不能算剧情还原通过。测试是独立浏览器上下文，不是 Android 真机。

以上是独立测试上下文发送真实 API/生图请求，不等于已验证真实正文模型的整段网络流。真实主页面安装加载另外检查，不在测试中替用户发送角色扮演消息。

安装检查：已通过酒馆 `/api/extensions/install` 安装公开仓库。实际酒馆主页中插件成功加载，430px 宽设置框无横向溢出，未捕获本插件 JavaScript 异常。已为独立导演读取现有 DeepSeek flash 连接，等待酒馆保存响应并从服务器重读确认成功；自动开关为关闭。安装前后原版智绘姬 `index.js` SHA-256 一致。

桌面 1280px 重绘预览检查：已确认过的提示词原样进入编辑框，不再次叠加画风词；改写后切换工作流再返回，编辑稿未丢失；预览无横向溢出。测试只打开预览，不额外提交生图。

## 人工画面检查

- 第一组：面部完整可见、银发绿眼和白裙到黑衣换装基本符合；旅行箱错误地被手提，第二张漏掉相机。不能算剧情还原通过。
- 加入“必要道具、空间关系、兼容取景”结构化镜头规划后，第二组相机出现、换装正确、面部可见；旅行箱仍被手提。不同随机种子的少量样本不能证明规划改动的因果效果。
- 较早双段样本：手动雨伞图有红伞、黑衣和可见正脸；自动相机图保留银发、绿眼与黑衣，但**漏掉银色相机**，手势也偏离剧情。原文定位与 prompt 均正确，属于成图服从失败。自动导演还曾在后段相机之后倒序补画前段更衣室；已加门槛与单元测试禁止这种回填。
- 后续独立双段实测：手动选择黑衣/红伞段，确认前 0 次 ComfyUI 请求且 0 个导演服装预设；确认后真实任务完成，智绘姬兼容格式的服装预设只关联该人物档案，图显示在选中段后。锁定后下一轮自动镜头在正文结束前提交，相机、人物正侧脸和黑衣可见；原有四个选项与 Swipe 隔离仍正常。自动图出现模型额外绘制的 `REC` 角标，不能视为文字/UI 完全受控。两图服装描述有轻微措辞差异，初测建了重复衣服；随后增加词项等价复用并加入回归测试，该去重修复尚未再生成一轮真图。
- Anima 原型实测：已知金色长发/蓝眼时，任务使用 `violet evergarden` 作单一视觉原型，同时保留剧情黑衣与红伞；真实 Miaomiao 图片有可见脸、黑衣与红伞。候选库很小，网站完整角色库没有同步进插件，不承诺每个人物都能自动命中合适 Tag。
- 小痣等细节与跨图脸型未证明稳定，部分构图未严格服从取景。当前没有图像语义验收模型，接口完成不等于画面合格。
- 发现重绘重复叠加画风的代码风险，已修复并加入回归测试。

## 未完成/不承诺

- Android 真机长按、跨段拖选及实际手机网络。
- 云端真实生图，真正的 Anima 人脸参考注入，人物局部裁切参考与跨图强一致性。
- 自动第二候选的保留/结尾提交已有单元回归，但长篇真实流式剧情、多角色动作与道具服从率尚无足够样本。
- 当前服务器保存工作流已核实，但不能确认用户浏览器未保存画布完全一致。
- 原版智绘姬没有无补丁任务快照公开接口；此版独立执行 ComfyUI/酒馆接口，不复用其私有生图队列/查看器。锁定为固定外貌描述，不是人脸模型锁。

自动默认关闭。此版可用于验证安装、选段确认、配套工作流与原位插图，不应标为“完美剧情一致性”或“完整验收版”。

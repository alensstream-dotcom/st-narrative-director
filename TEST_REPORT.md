# 验收记录（2026-09-25）

## 当前结果

- `npm test`：77 项通过；`git diff --check` 通过。
- 对接真实酒馆 1.18.0 和智绘姬 3.1.2：导演曾在正文结束前交付英文提示词，智绘姬自动点击并由 ComfyUI 生成、回填 896×1152 图像。另一次真实回合在正文结束后约 1 秒才交付，仍成功出图。因此“每一轮必定在正文结束前交付”**尚未达成**。
- 原先的“黑图”截图截取的是错误元素；检查实际的智绘姬图片节点后，图像可见且尺寸正常。
- 设置页桌面 1280px 和触屏手机 360px 模拟检查通过：API Key、模型下拉与手填框可见，无横向溢出、无插件脚本错误。模拟酒馆模型状态接口首次返回 HTTP 200 错误包、第二次返回列表后自动显示模型；模拟连续代理失败后，本页密钥直连 `/models` 成功。选中下拉模型和手填模型均写入导演配置；只刷新列表不改变导演配置，也不调用插件的保存方法。酒馆页面初始化仍可能自行发起全局设置保存。

## 失败原因与对应处理

| 已观察的失败 | 原因 | 当前处理 |
| --- | --- | --- |
| 选择模型时列表有时完全加载不了 | 酒馆代理从上游取 `/models` 失败时会返回 HTTP 200 加 `{error:true}`；先前界面只给泛化错误，且未保留列表 | 识别错误包，短暂重试一次，支持多种兼容接口列表形状；当前页密钥在代理失败时尝试直连 `/models`，保留成功列表并始终允许手填 ID |
| 手机设置页没有直观的连接和模型选择入口 | 旧界面把直接输入 API Key 藏在密钥来源选项里，主模型和自动模型配置不够清晰 | 参考智绘姬的 LLM 设置，将地址、Key、模型下拉、手填框和「连接并获取模型」放在主界面；自动模型及密钥管理移入高级项 |
| 刷新/测试时可能重写其他酒馆设置 | 旧界面即使只是读取模型，也调用 `saveSettingsDebounced()`；多个标签页可能互相覆盖旧的正文设置 | 只在表单值变化时保存；模型列表缓存放在浏览器当前会话，不写入酒馆全局设置 |
| 某一轮没有图 | 正文模型在 30,000 token 上限内输出了约 84,328 字符的思考、0 字符可见正文，因 `length` 截断 | **保持正文推理强度 `max`**；预设和当前活动输出上限均已保存为 64,000，刷新后复核通过。若另一个旧页面重新保存全局设置，仍可能覆盖活动值；无可见正文时导演不能编造画面 |
| 有图但没有严格赶在正文结束前 | 辅助 API 一次场景分析约 4–5 秒，短回复可在同一时间内结束 | 在足够可靠的正文出现时提前并行分析，安全地接受完整场景片段；尾段兜底仍出图，但不把晚交付算作中途成功 |

## 边界

这些检查不等于 Android 真机验收，也不保证远端模型和 ComfyUI 每次可用。Anima 角色 Tag 新增鸣潮与明日方舟候选；仅唯一匹配有证据的固定外貌才自动应用，多个相似候选需要手选。Tag 不能保证像素级形象一致，人脸、衣着和空间关系仍受图像模型随机性影响。工具只根据可见正文和已确认资料选画面，正文模型未生成故事时不会把隐藏思考当作画面来源。先前测试使用的角色对话与图片保留在本地忽略目录，不提交到公开仓库。

仓库 `0.4.3` 已通过酒馆扩展更新接口安装，安装目录提交号为 `f48e00f`。安装态通过同样的桌面与触屏手机模拟检查。独立只读检查确认正文推理强度仍为 `max`、输出上限为 `64000`。直接复制到 `Program Files` 曾被 Windows 拒绝，因此使用酒馆扩展更新接口，而非更改目录权限。

# 设计取舍与证据

## 原版智绘姬不改文件

用户明确要求保持原版插件可更新，并让原版智绘姬负责实际生图。导演把英文提示词以 `image###...###` 标记插入对应原文的显示区域，调用其现有识别与自动点击流程；智绘姬读取自己的工作流、队列、缓存并显示图片。导演不修改其文件，也不把标记混入聊天正文。人物档案按 ID 关联或新增并记录 `directorOwner`。源码和许可证核查见 `INTEGRATION_NOTES.md`。

## 手动先确认，自动不凑数

手动选段由浏览器原生 Selection 提取原文锚点，导演 API 可为多事件选段提供至多三个候选；用户只确认其中一个并可改 prompt。确认前浏览器测试记录 0 次生图请求。自动最多两张，第二张只接受已提交镜头之后的事件；不倒序补画较早的低价值画面。这样可能只生成一张，符合用户不凑数的要求。

## 人物稳定性的实际边界

固定外貌从原版档案、角色卡与有证据的剧情渐进读取；动态服装按原文时点分析，用户改过的原档案不会自动覆盖。用户满意后锁定的是已核实的固定描述，不是实际人脸参考模型。当前 Miaomiao 工作流未验证可注入参考图，因此跨图脸部完全一致不是已交付能力。
智绘姬原版的 `characterPresets[profileId].outfits` 引用 `outfitPresets`。导演只在任务确认/自动批准后写入有原文依据的服装预设；同人物同词项服装复用，换衣建立独立预设，不修改全局启用列表或用户手改数据。时点服装 ID 保存在图任务中，正文仍不写 prompt/记忆。ANIMADEX 是已核实的 Anima Tag 检索站点；当前提供外部查找入口和小型离线候选，不在首图链路实时访问它，以免速度和第三方可用性决定是否出图。

## 成图不能只看任务状态

真实 Miaomiao 测试中，导演给出的相机画面与英文 prompt 都正确，但一张自动图漏掉相机。模型完成不等于故事画面合格。当前缺少已验证的视觉复核器；人工可通过图片旁重绘入口修正，但还不能自动识别并修复每个道具/动作失误。测试详情见 `TEST_REPORT.md`。

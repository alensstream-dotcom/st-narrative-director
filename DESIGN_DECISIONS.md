# 设计取舍

1. **不修改原版优先。** 原候选最小补丁被用户明确否决。改用独立快照执行适配，避免全局设置切换和更新覆盖。代价是无法共享原版私有队列/显示函数，文档与 UI 不假装共享。
2. **先有事件，再有镜头。** 一次结构化导演请求返回原文依据、单一动作阶段、人物位置、时点状态与英文画面描述。动作未发生/负面自检/引用失配拒绝提交，不往关键词编译器堆规则。
3. **不靠负面词解决取景。** 首次亮相要求在剧情允许时展示完整可辨认面部与头顶空间；明确背身/身份隐藏则尊重原文。台词转为表情动作，不让扩散模型生成错乱英文对白。
4. **低侵入图片。** 不往 `mes` 加中括号 prompt；图片保存在酒馆服务器，锚点在 `extra`。渲染时插入原段位置，保留原有按钮。
5. **不承诺无法做到的人脸一致。** 当前可用的是固定特征锁定。模型/工作流支持参考图和人物区域后才可启用更强的参考策略；不自动下载模型，不用整张双人图锁两个人。
6. **未知就不擅自补全。** 人物新建仅填有证据的固定特征；已有用户档案不覆盖、不自动加入启用列表。服装与伤势随原文时点变化。
7. **使用原生密钥 ID。** 酒馆写入密钥会改变默认 active secret，因此本插件不调用写入接口，不影响正文连接。手工输入密钥仅本页临时使用。
8. **自动图额度不是 KPI。** 最多两张，不凑数；流式第二张保留给更重要事件。手动始终先预览，不占自动名额。
9. **取消不抢占其他任务。** 停止插件排队/显示；不调用全局 ComfyUI interrupt。费用/已经开始的 GPU 任务无法保证撤销。

# 任务续接摘要

来源任务：制作 ST 文生图插件（019f2354-850f-7df1-af52-60a149deccda）。仓库：`D:\CodexWorkspace\st-narrative-director`。

用户的最新要求：一次性综合修复，从过去多轮失败中找根因；参考本机智绘姬源码；模型列表及界面需要真正可用；**正文推理强度不得调低**；不要反复跑成本高的剧情生成测试。

插件负责从可见剧情中选择有证据的画面，产生英文提示词；原版智绘姬负责 `image###...###`、ComfyUI、生图和显示。不修改智绘姬源码。自动模式每轮最多两图，尽量至少一图、中途交付；没有可画正文或服务失败时不编造。手动生图需用户确认。独立辅助 API 当前选用兼容接口 `https://api-slb.krill-code.net/v1`、`gpt-6-sol`、酒馆保存的密钥引用；密钥值不能进入仓库或报告。正文为 DeepSeek，推理强度 `max`。智绘姬工作流为 Miaomiao Harem，896×1152。

本轮从智绘姬源码学习其模型列表处理：使用酒馆 `/api/backends/chat-completions/status` 代理，接受多种响应形状，保留模型选择和手填入口。酒馆自身在上游 `/models` 失败时可能回 HTTP 200 错误包。旧导演设置页未妥善处理这一点，还在只读刷新时保存整个酒馆设置。现已集中改造连接设置页、短暂重试、会话内列表缓存、当前页 Key 的直连 `/models` 回退和明确提示。直接输入的 Key 仅保存在当前页内存；酒馆密钥引用保留在高级选项。

Anima 原型新增鸣潮长离、今汐和明日方舟斯卡蒂、阿米娅、能天使、德克萨斯、拉普兰德的角色 Tag。按有证据的固定发色、瞳色、发长与耳朵/光环等特征匹配；只有唯一候选才自动套用，多个候选需手选。Tag 是形象参考，不覆盖当前剧情衣着、动作、地点，锁定仍由用户确认。

以前真实测试曾成功在正文结束前交付提示词并实际回填图像；另一些回合因辅助 API 比短正文慢而晚交付。另一次正文在 30,000 token 上限内耗尽于隐藏思考，0 可见正文。这些失败不能通过降低正文推理强度“解决”；当前预设及活动输出上限已提高至 64,000，推理强度仍为 `max`，刷新后核对通过。见 `TEST_REPORT.md`，不可宣称连续两轮严格中途交付已通过。

本轮针对性验证：77 项单元检查；桌面和 360px 触屏手机模拟设置页；模型代理首次错误后重试成功及连续错误后的直连回退；下拉选择和手填均能更新模型；只读刷新没有全局设置保存；没有再反复跑真实剧情。版本 `0.4.3` 待通过酒馆扩展更新接口安装并核对安装态。

# 任务续接摘要

来源任务：制作 ST 文生图插件（019f2354-850f-7df1-af52-60a149deccda）。仓库：`D:\CodexWorkspace\st-narrative-director`。

用户的最新要求：一次性综合修复，从过去多轮失败中找根因；参考本机智绘姬源码；模型列表及界面需要真正可用；**正文推理强度不得调低**；不要反复跑成本高的剧情生成测试。

插件负责从可见剧情中选择有证据的画面，产生英文提示词；原版智绘姬负责 `image###...###`、ComfyUI、生图和显示。不修改智绘姬源码。自动模式每轮最多两图，尽量至少一图、中途交付；没有可画正文或服务失败时不编造。手动生图需用户确认。独立辅助 API 当前选用兼容接口 `https://api-slb.krill-code.net/v1`、`gpt-6-sol`、酒馆保存的密钥引用；密钥值不能进入仓库或报告。正文为 DeepSeek，推理强度 `max`。智绘姬工作流为 Miaomiao Harem，896×1152。

本轮从智绘姬源码学习其模型列表处理：使用酒馆 `/api/backends/chat-completions/status` 代理，接受多种响应形状，保留模型选择和手填入口。酒馆自身在上游 `/models` 失败时可能回 HTTP 200 错误包。旧导演设置页未妥善处理这一点，还在只读刷新时保存整个酒馆设置。现已集中改造连接设置页、短暂重试、会话内列表缓存和明确提示。

以前真实测试曾成功在正文结束前交付提示词并实际回填图像；另一些回合因辅助 API 比短正文慢而晚交付。另一次正文在 30,000 token 上限内耗尽于隐藏思考，0 可见正文。这些失败不能通过降低正文推理强度“解决”；当前预设输出上限已提高至 64,000，但活动设置需要最终核对。见 `TEST_REPORT.md`，不可宣称连续两轮严格中途交付已通过。

本轮针对性验证：74 项单元检查；桌面和 360px 手机设置页；模拟首次模型列表错误、重试成功；刷新没有全局设置保存；没有再反复跑真实剧情。下一步是最后安装/版本同步及必要的安装态轻量检查。

# 任务续接摘要

来源任务：制作ST文生图插件（019f2354-850f-7df1-af52-60a149deccda）。最近用户要求优先于早期方案。

## 最终目标与约束

- 独立剧情导演扩展，支持手机酒馆安装与迁移，不改正文预设或智绘姬源代码。
- 导演在正文流式生成过程中选取有原文依据的镜头，产出英文提示词；原版智绘姬通过 `image###...###` 识别、自动点击、调用 ComfyUI、回填图片。
- 自动每轮尽量至少一张、最多两张；优先中途交付。已有中途图后不在结尾重扫补第二张。无可画内容或服务失败不能编造画面保证数量。
- 手动跨段选择，先确认镜头和提示词再出图，次数独立。
- 人物固定外貌与当前服装分开管理，有证据才更新，保留用户手改和锁定；关联智绘姬原生人物/服装功能。
- Miaomiao Harem 配套 UNet + Turbo LoRA 0.8，12 步，CFG 2.5，Euler/simple，896×1152。以工作流中的 UNet 为准。
- 用户允许用现有角色卡与聊天做真实测试。最新验收要求已从十轮改为连续两轮；有错先修，再重测两轮。不能仅以单测或图片按钮出现算通过。

## 位置

- 源码：`D:\CodexWorkspace\st-narrative-director`
- 仓库：https://github.com/alensstream-dotcom/st-narrative-director
- 酒馆：http://localhost:11451/
- 安装目录：`C:\Program Files\SillyTavern Launcher GUI\data\st_data\default-user\extensions\st-narrative-director`
- API 使用酒馆已保存密钥引用。禁止将密钥值写入仓库、测试报告或摘要。

## 本次接续集中处理的缺口

- 原任务未提交修复：隐藏思考/梦鲸正文提取、流式前缀重写导致锚点失效、链式生成结束事件、网关错误识别、流式优先输出提示词、独立自动模型选择。
- 测试输入曾被重复赋值为字面 Unicode 转义串，现使用正常中文。
- 自动模型设置原脚本只检查内存；现等待酒馆 settingsReady、成功保存响应、刷新后再次核对，另支持验收模型前置断言。
- 人物性别曾未进入流式视觉记忆，导致已知女性首图被拒；已补传。
- 首图提前交付后，完整人物/服装元数据曾未回填已交付记录；现合并到同一张图并同步有证据的服装。
- 酒馆 1.18.0 仅对白名单模型转发顶层 `reasoning_effort`，此前给 `gpt-6-sol` 的低推理参数被丢弃。兼容接口现通过原生 `custom_include_body` 转发参数，无需修改酒馆或智绘姬源码。
- 2026-09-25 最后一次两轮测试：第一轮中途交付且图完成；第二轮正文 API 只生成 84,328 字符思考，0 正文，finish_reason=length。已向用户询问能否调低正文推理，尚待回答；正文模型/预设未改。中转站另实测接受 none/low，拒绝 minimal。

## 验收方法

`npm test` 检查规则回归。`node tools/live-ten-rounds.cjs --rounds=2 --expect-stream-model=gpt-6-sol` 使用实际角色卡、正文 API、导演 API、智绘姬和 ComfyUI，检查每条自动提示词在回复结束前交付、英文、回调成功、每张图片实际加载及正文未混入标记。默认在测试页加载工作区源码；加 `--installed` 才直接验证实际安装文件。测试结束恢复新增聊天与测试资料。

验收结果以 `TEST_REPORT.md` 和本地忽略的 `artifacts/live-ten-rounds.json` 为准。提示词中途交付不等同于 GPU 图片在正文结束前完成；这两个时间不可混称。安卓真机与跨设备迁移尚未验证。
